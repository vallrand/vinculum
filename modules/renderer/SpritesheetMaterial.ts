import { Manager } from 'framework/Manager'
import { Store } from 'loader/Store'
import { Material, MaterialOptions } from './Material'

export class SpritesheetMaterial extends Material {
    setup(options: MaterialOptions, manager: Manager){
        super.setup(options, manager)
    }
}